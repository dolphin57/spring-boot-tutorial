spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: mybatis-sensitive
  datasource:
    url: jdbc:mysql://localhost:3306/general_data?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 123456

# mybatis-plus映射mapper文件
mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.eric.sensitive.entity